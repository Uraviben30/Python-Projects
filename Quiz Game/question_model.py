class Question:

    def __init__(self,q_text, q_answer):
        # Initialize the question with its text and correct answer
        self.text = q_text        # Stores the text of the question
        self.answer = q_answer    # Stores the correct answer (True/False)
